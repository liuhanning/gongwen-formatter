# AGENTS.md — 代理导航入口

## 项目定位

公文格式化工具（纯 VBA 宏），将 Word/WPS 文档一键排版为符合 **GB/T 9704** 党政机关公文格式或政府交付版标准的文档。
技术栈：VBA（无 Python/Node 运行时依赖），目标宿主为 Microsoft Word 2010+ 和 WPS Office 2019+。

## 目录结构路由

```
wordhong/
├── GongwenFormatter.bas          # ★ 主实现（Word 版，~1600 行）
├── GongwenFormatter_WPS.bas      # ★ WPS 兼容版（与主版同步维护）
├── ResearchProjectFormatter.bas  # 课题研究格式化（独立模块，~560 行）
├── demo_gongwen.docx             # 示例/测试文档
├── 标准格式.docx                 # 格式基准参考
├── README.md                     # 完整使用文档 + 格式规范（验收标准）
├── CHANGELOG.md                  # 版本日志
├── 公文格式要求.md               # 格式需求来源
├── 咨询报告格式规范.md           # 咨询报告补充规范
├── 政府部门格式标准.txt          # 政府交付版参数
├── 格式模式说明.md               # 双模式对比
├── WPS兼容性说明.md              # WPS 差异说明
├── tests/                        # Python 辅助脚本 pytest 测试
└── *.py                          # 辅助脚本（比较/修复，非核心）
```

## 核心文件说明

| 文件 | 职责 | 入口宏 |
|------|------|--------|
| `GongwenFormatter.bas` | Word 版主实现：封面识别、标题分级、正文/表格/页码格式化 | `FormatGongwen` |
| `GongwenFormatter_WPS.bas` | WPS 版，按咨询报告标准稿口径直接执行，无模式弹窗 | `FormatGongwen` |
| `ResearchProjectFormatter.bas` | 课题研究格式（GB/T 7713 风格），独立于公文逻辑 | `FormatResearchProject` |

修改任何 `.bas` 时注意：
- 两个公文版本（Word/WPS）需保持逻辑同步，除非差异是平台兼容需要。
- 代码使用 GBK 编码保存，UTF-8 会导致中文注释乱码。

## 高风险区域

### 1. 封面识别逻辑（`DetectCoverLayout`，约 L508-L600）

- 扫描文档前 30 段，收集短段落（≤100 字符）为封面候选。
- 从后向前识别日期段（支持 `2026 年 4 月`、`2026-04`、`2026/04`、`2026.04` 等格式）。
- 日期前识别编制单位（机构关键词匹配，未命中则回退到日期前一候选段）。
- 编制单位前的所有候选段视为封面标题（支持多行）。
- **风险**：段落长度阈值、日期正则、机构关键词列表的改动会直接影响封面/正文分界。

### 2. 字体退化机制（`GetFont` / `FontExists`，约 L1193-L1210）

- `GetFont(ParamArray fonts())` 按优先级返回系统中存在的第一个字体。
- 典型退化链：`方正小标宋简体 → 华文中宋 → 黑体`、`仿宋_GB2312 → 仿宋 → 宋体`。
- **风险**：退化顺序错误会导致标题字体不符合规范；`FontExists` 在不同 Word/WPS 版本下行为可能不同。

### 3. 标题序号识别与层级映射

- 依赖正则前缀（`一、`、`（一）`、`1.`、`（1）`、`①`）判断层级。
- 半角 `.` 与全角 `．` 的混用是已知 bug 来源。

### 4. 页码域代码

- 目录页使用罗马数字页码，正文使用阿拉伯数字，从 1 开始。
- 域代码后不可有空白字符（已知 pitfall）。

## 验收标准引用

格式规范的权威来源为 [README.md](./README.md) 中以下章节：

- **§格式规范（7项要求）**：页面设置、标题格式、标题序号、正文格式、页眉页码、表格格式、插图格式。
- **§封面识别规则**：扫描范围、日期/编制单位识别、多行标题。
- **§补充规则（2026-04-06）**：表格边框/内边距、目录与索引格式。
- **§补充规则（2026-04-15）**：封面识别补充（前 30 段、非连续布局）。

修改格式逻辑后，须对照上述章节验证输出文档是否仍满足全部 7 项要求。

## 开发约定

- 修改 `.bas` 后在 Word 和 WPS 中各运行一次 `FormatGongwen` 验证。
- 新增/变更格式参数时同步更新 README.md 对应章节。
- Python 辅助脚本（`compare_*.py`、`fix_*.py`）为一次性工具，不纳入核心维护。

## 验证

### Python 辅助脚本测试

```bash
.venv\Scripts\python.exe -m pytest tests/ -v
```

### VBA 宏手动验证检查清单

由于 VBA 宏无法自动化单元测试，每次修改 `.bas` 文件后须按以下清单手动验证：

#### 1. 导入无编译错误

- [ ] 打开 Word/WPS，按 `Alt + F11` 进入 VBA 编辑器
- [ ] 文件 → 导入文件 → 选择目标 `.bas` 文件
- [ ] 确认导入后无"编译错误"弹窗
- [ ] 在 VBA 编辑器中执行 **调试 → 编译 VBAProject**，确认无语法错误
- [ ] 检查模块树中新增模块名称正确，无重复模块

#### 2. 执行主宏无运行时错误

- [ ] 打开测试文档 `demo_gongwen.docx`（或任意含多级标题的 .docx）
- [ ] 按 `Alt + F8`，选择 `FormatGongwen`（或 `格式化公文`）
- [ ] 点击"运行"，确认：
  - [ ] 无运行时错误弹窗（如"运行时错误 91"、"运行时错误 5941"等）
  - [ ] 宏正常执行完毕，无中断
- [ ] 对 `GongwenFormatter.bas`：确认模式选择对话框正常弹出且选项可读（无乱码）
- [ ] 对 `GongwenFormatter_WPS.bas`：确认直接执行无弹窗报错

#### 3. 格式结果符合 README 规范

对照 `README.md` 中"格式规范（7项要求）"逐项检查：

- [ ] **页面设置**：A4 版幅，上3.7cm/下3.5cm/左2.8cm/右2.6cm，页眉页脚1.8cm
- [ ] **标题格式**：
  - [ ] 一级标题：方正小标宋简体（或退化字体）二号，30磅行距，居中
  - [ ] 二级标题：黑体三号，30磅行距，缩进正确（标准模式=左缩进2字符，政府模式=首行缩进2字符）
  - [ ] 三级标题：楷体_GB2312三号，30磅行距
  - [ ] 四级标题：仿宋_GB2312三号，28磅行距
- [ ] **正文格式**：仿宋_GB2312三号，首行缩进32pt，两端对齐，段前段后0
- [ ] **页眉页码**：页码宋体四号底部居中，正文从1开始
- [ ] **表格格式**：表题黑体小四居中，内容仿宋_GB2312约10.5pt加粗，边框正确
- [ ] **封面识别**：封面标题、编制单位、日期正确识别并应用对应样式

#### 4. 辅助宏验证（可选）

- [ ] `FormatSelectedParagraphs`：选中段落后运行，仅格式化选中内容
- [ ] `FormatAllTables`：表格格式正确应用
- [ ] `ReplaceSymbols`：中英文标点正确转换

### 验证环境

| 项目 | 要求 |
|------|------|
| Microsoft Word | 2010 或更高版本 |
| WPS Office | 2019 或更高版本 |
| 测试文档 | `demo_gongwen.docx` 或含多级标题+表格的文档 |
| 字体 | 建议安装方正小标宋简体、黑体、楷体_GB2312、仿宋_GB2312 |
